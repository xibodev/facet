{
  "name": "facet-remotion-composer",
  "version": "2.0.0",
  "description": "Facet-owned Remotion explainer composer",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "remotion studio src/index.tsx",
    "typecheck": "tsc --noEmit",
    "build:test": "tsc",
    "test:contract": "npm run build:test && node --test tests/contract.test.mjs",
    "test:legacy": "node --test tests/legacy-guard.test.mjs",
    "test:render": "node --test tests/render.test.mjs",
    "test": "npm run test:contract && npm run test:legacy && npm run test:render"
  },
  "dependencies": {
    "@remotion/cli": "4.0.484",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "remotion": "4.0.484"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "typescript": "^5.3.0"
  }
}
