# Legacy compatibility fixtures

These files verify the stable refusal returned for an input shape accepted by
older module descriptors. They are negative compatibility tests, not host
guidance, capability examples, or a supported lifecycle.
